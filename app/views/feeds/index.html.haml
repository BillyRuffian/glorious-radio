= turbo_frame_tag 'content', data: { turbo_action: 'advance' }  do

  %h1
    All podcasts 

  .grid.grid-cols-5.gap-2
    = render partial: 'feed', collection: @feeds


  %h1 My podcasts
  %ul 
    - @subscribed.each do |f| 
      %li 
        = link_to f.name, feed_path(f)